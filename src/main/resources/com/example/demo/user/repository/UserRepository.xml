<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.user.repository.UserRepository">

    <select id="findUserByEmail" resultType="com.example.demo.user.entity.UserEntity">
        SELECT id, username, email, address, password
        FROM user_table
        WHERE email=#{email}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*)
        FROM user_table
        WHERE email=#{email}
    </select>

    <insert id="register">
        INSERT INTO user_table
        (id, username, email, address, password )
        VALUES
        (#{id}, #{username}, #{email}, #{address}, #{password})
    </insert>

    <update id="updateUser">
        UPDATE user_table
        SET
        email = #{email},
        username=#{username},
        address = #{address}
        WHERE id = #{id}
    </update>

    <update id="updatePwd">
        UPDATE user_table
        SET
        password = #{password}
        WHERE email = #{email}
    </update>
</mapper>