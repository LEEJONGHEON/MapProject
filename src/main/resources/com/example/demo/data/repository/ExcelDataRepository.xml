<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.data.repository.ExcelDataRepository">

    <insert id="uploadData">
        INSERT INTO excel_data
        (place, phoneNumber, roadAddr, jibyeonAddr)
        VALUES
            (#{place}, #{phoneNumber}, #{roadAddr}, #{jibyeonAddr})
    </insert>

    <select id="searchByRoad"  resultType="com.example.demo.data.entity.ExcelDataEntity">
        SELECT * FROM excel_data
        WHERE roadAddr LIKE CONCAT('%',#{search},'%')
    </select>

    <select id="searchByJibyeon"  resultType="com.example.demo.data.entity.ExcelDataEntity">
        SELECT * FROM excel_data
        WHERE jibyeonAddr LIKE CONCAT('%',#{search},'%')
    </select>
</mapper>